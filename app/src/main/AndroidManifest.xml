<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".constraint_layout.ConstraintActivity"></activity>
        <activity android:name=".event_conflict.EventConflictActivity" />
        <activity android:name=".event_conflict.EventConflictInnerActivity" />
        <activity android:name=".CustomLayoutManagerActivity" />
        <activity android:name=".FlowActivity" />
        <activity android:name=".TestLayoutActivity" />
        <activity android:name=".event_conflict.srl_vp.SRL_VP_main" />
        <activity android:name=".NestedScrolling.nested_scroll_parent_child.NestScrollActivity" />
        <activity android:name=".NestedScrolling.NestedTraditionActivity" />
        <activity android:name=".coordinatorLayout.CustomBehaviorActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnimatorActivity" />
    </application>

</manifest>